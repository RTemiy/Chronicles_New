import { statsManager } from '../../index'
import { EStoriesEn } from '../../Utils/EStoriesNames'

statsManager.add({
  category: 'Choice',
  id: 'Difficulty',
  story: EStoriesEn.HF,
  idDescription: 'Сложность'
})
