import { statsManager } from '../../index'
import { EStoriesEn } from '../../Utils/EStoriesNames'

statsManager.add({
  category: 'Choice',
  id: 'DreamForest',
  idDescription: 'Было в лесу во сне',
  story: EStoriesEn.ISML
})
