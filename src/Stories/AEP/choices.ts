import { statsManager } from '../../index'
import { EStoriesEn } from '../../Utils/EStoriesNames'

statsManager.add({
  category: 'Choice',
  id: 'TakeJohnGun',
  story: EStoriesEn.AEP
})

statsManager.add({
  category: 'Choice',
  id: 'HitJohn',
  story: EStoriesEn.AEP
})

statsManager.add({
  category: 'Choice',
  id: 'TurnAway',
  story: EStoriesEn.AEP
})
